### /commit コマンドのプロンプト

まだコミットしていない変更内容を網羅的に確認して、日本語でどんな修正をしたのかをわかりやすくまとめる。その後、以下の手順を実行する：

1. `git add .` で変更をステージする
2. `git commit` のコメントとしてその内容を反映する
3. `git push` でリモートリポジトリにプッシュする
4. プルリクエストを作成する（GitKraken MCP ツールまたは GitHub CLI を使用）
